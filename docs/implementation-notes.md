# Implementation Notes

- Keep site static (no database).
- Use filesystem for MDX content.
- Add helpers in `lib/posts.ts` and `lib/projects.ts` for loading.
- Generate static routes for posts and projects.
- Add SEO meta tags for each page.
- Use Vercel Analytics (no consent required).
